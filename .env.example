# PostgreSQL Database Configuration
# Nonprofit Fund Accounting v8.8 - Cross-platform configuration

# Database connection settings
PGHOST=localhost
PGPORT=5432
PGDATABASE=fund_accounting_db
PGUSER=npfadmin
PGPASSWORD=npfa123

# Application settings
NODE_ENV=development
PORT=3000

# Optional settings
DEBUG=false
SESSION_SECRET=replace_with_random_string

# Platform-specific notes:
# - Mac/Windows: Use npfadmin/npfa123 (created by setup-database.sql)
# - Ubuntu: Can use npfadmin/npfa123 OR postgres/(no password)
# - All platforms: Ensure PostgreSQL is running on port 5432
